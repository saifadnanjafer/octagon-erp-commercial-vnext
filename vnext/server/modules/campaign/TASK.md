# R6.7 Omni-communications (WhatsApp campaign engine)

Campaign dispatch, messaging template interpolation, logging, DLR and inbound webhooks.

Frozen boundaries:
- Existing `partner_master` and loyalty cards remain canonical.
- Inbound customer responses log without modifying tenant properties.
- Strict row-level company scoping.
