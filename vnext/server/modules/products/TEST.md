# Verification

`node scripts/test-r3-core.mjs` covers canonical product reuse, goods/services, duplicate barcode, UOM, cross-company scope, deterministic precedence, and explain trace. `node scripts/test-r3-http.mjs` covers authenticated HTTP and the RTL responsive entry point.
