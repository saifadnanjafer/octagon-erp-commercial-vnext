# R7.1 Shop-floor execution terminals

Kiosk operators PIN-badge logins, operation time tracking (start/pause/finish), material issue consumption, and scrap logging.

Frozen boundaries:
- Existing `mrp_work_order`, `mrp_scrap`, and `stock_move` remain canonical.
- Safe timesheet and log recordings.
- Strict row-level company scoping.
