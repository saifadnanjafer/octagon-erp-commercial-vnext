# Verification

Disposable R3 tests cover scoped reservations, inventory records, and explicit online-only posting boundaries. Barcode scans and drafts are safe to capture; sensitive stock posting is not enabled offline.
