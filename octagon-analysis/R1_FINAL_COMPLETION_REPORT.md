# Octagon Commercial VNext — R1 Final Completion Report (Corrected, All 19 Canonical Tasks)

Supersedes `R1_FORENSIC_AUDIT_REPORT.md` and `R1_REMAINING_WORK_MATRIX.md` for closure purposes. Both prior documents independently omitted **T1.9.2** from their task tables (18 rows instead of 19) and both trusted claims that this audit disproves in places. This report is built from a fresh, evidence-only re-read of the current codebase — one verification pass per lane (A/B/C/D), each citing file:line — run *after* the 2026-07-17 ~14:07–14:14 repair pass. No claim below is carried over from a prior doc without independent confirmation.

**Verdict: R1 is NOT closed.** 6 of 19 tasks are clean PASS, 8 are PARTIAL, 5 are FAIL/MISSING — including a live regression that breaks CRUD updates for every entity. The kernel-demo exit gate has not yet been run (see §3) and cannot pass in the current state.

---

## 1. Corrected 19-Task Matrix

| Task ID | Epic | Verdict | Evidence | Gap (if not PASS) |
|---|---|---|---|---|
| **T1.1.1** | Collection/field registry | **PASS** | `migrations/101_r1_lane_a_tables.mjs:6-101` creates `x_records`/`collection_registry`/`field_registry`, seeded with `crm_lead`/`helpdesk_ticket`/`product`; read by `crud-engine.js:loadRegistry()` (17-46); mounted `server.js:2849`, routed `server.js:1827`. | Minor: no HTTP-level registry-driven-CRUD test exists. |
| **T1.1.2** | Server CRUD verbs + filter compiler | **FAIL (regression)** | Create/read/list/summary work; filter compiler is allow-listed and param-bound (`crud-engine.js:273-326`). | `updateRecord()` at `crud-engine.js:215-254` references `cfg` at line 242, but `cfg` is never in that function's parameter list (`entity, id, patch, user, companyId`) — every `PATCH /api/x/:entity/update/:id` throws `ReferenceError`, caught generically, returned as HTTP 500. **Every entity update is broken.** No injection-fuzz or 10k-list perf test exists. |
| **T1.1.3** | Client renderer + section-shell | **PARTIAL** | `vnext/client/ui-crud.js` (733 ln, config-driven mount) + `vnext/client/entity-ui-registry.js` (configs for the 3 seeded entities) exist and are well-formed; server's static handler (`server.js:2634-2676`) is an unrestricted `path.join(__dirname,...)` + `fs.readFile`, so the JS/CSS files are reachable by direct URL — no routing gap. | No page actually includes/mounts the entity UI and is reachable via navigation (no nav link, no confirmed working demo entry point) — the UI is unexercised by an actual browser user journey. |
| **T1.2.1** | ACL schema + middleware + deny-by-default | **PASS** (evidence-limited) | `acl-engine.js:99-119` — no loopback bypass, 401 on null session (`147-175`); mounted `server.js:2850`, invoked in pipeline before legacy routes `server.js:1821-1825`. | No route-coverage scanner exists anywhere in `scripts/` to mechanically prove "100% of routes gated" — verified by manual read only. |
| **T1.2.2** | Row-scope + field masks | **PASS** | `acl-engine.js:284-332` (`maskValue`/`maskFields`/`checkForbiddenWrites`) against `x_acl_field_rules` (`migrations/501_r1_kernel_completion.mjs:9-15`); wired into write-reject (`crud-engine.js:166,220`) and read-mask (`crud-engine.js:385,486`); admin routes `acl-engine.js:242-276`; tested `scripts/test-vnext-kernel-completion.mjs:151-177`. | — |
| **T1.3.1** | Doc-state engine + transitions | **PARTIAL** | `vnext/server/state/doc-state.js` (204 ln) real and mounted (`server.js:2856,1820`); illegal transition → 409 (`doc-state.js:83-85`); role + maker-checker guards (88-99); optimistic-concurrency 409 (101-105); migration `501:17-28`; tested `test-vnext-kernel-completion.mjs:78-147`. | No full-graph reachability/cycle validation when a state-def is registered (`doc-state.js:173-180` stores arbitrary JSON unchecked). No posted-immutability enforcement anywhere in `crud-engine.js` — a record in a terminal/posted state can still be freely `PATCH`ed (independent of the T1.1.2 bug). Both are named roadmap invariants (R1.3: "posted documents immutable"). |
| **T1.4.1** | Atomic + gapless-hashed numbering | **PASS** | `sequences.js:65-103` (`hashRecordChain`/`verifyChain`, SHA-256 prev-hash chaining); columns added `migrations/501:65-67`; called live on create+update `crud-engine.js:5,206,246`; tested `test-vnext-kernel-completion.mjs:181-205`. | — |
| **T1.4.2** | Audit trail + history | **PARTIAL** | `audit.js` append-only (`writeAudit` 13-40, INSERT-only, confirmed no UPDATE/DELETE anywhere on `x_audit`); `getHistory`/`diffObjects` (84-121); live route `GET /api/x/audit/:entity/:id` (`crud-engine.js:419-434`, mounted `server.js:2849`). | No client-side history panel — zero matches for `history`/`audit` across `vnext/client/**`. Backend is solid; the UI half of the acceptance criterion is unmet. |
| **T1.5.1** | Custom + snapshot fields | **FAIL** | `custom-fields.js` (249 ln) handles standard field CRUD/merge/export fine. | Snapshot fields are **entirely absent** — `TYPES` set (line 10) has no `snapshot` type; no materialize-on-transition hook anywhere in `vnext/` (grep for `materializ\|freeze\|snapshot_field` returns nothing relevant). This is a total miss, and the repair pass did not touch it at all — same gap as the pre-repair audit. |
| **T1.6.1** | Chatter + attachments ACL | **PARTIAL** | Thread/followers/auto-log solid, mounted live (`chatter.js`, `server.js:2851,1825`; `subscribeCrudChatter` 313-348). | Notification is follower-based, not `@mention` parsing (zero hits for `mention` in `vnext/`). **No attachment storage/ACL exists at all** — zero hits for `attachment` in `vnext/`, despite roadmap R1.6 specifying `vnext-data/files/` + metadata table + record-ACL-gated access. "Unauthorized attachment → 403" is unimplementable because there is no attachment feature yet. |
| **T1.7.1** | Saved views + worklists + Home workbench | **PARTIAL** | `x_views` table exists (`migrations/201_r1_lane_b_tables.mjs:59-67`), referenced in `ui-crud.js`/`excel.js`. | No server-side scoped worklist-count aggregation found; `home-widgets.js` is a localStorage favorites/finance-snapshot widget, not a worklist-count engine. No test evidence for "scoped counts correct." |
| **T1.8.1** | Notification center + adapters | **PARTIAL** | `notify.js` (156 ln): stub `emailAdapter`/`whatsappAdapter` (31-51, log to `vnext-data/outbound-*.log`); `send()` reads `x_notification_preferences.channels` (53-87); `PUT /api/x/notify/preferences` mounted `server.js:2852`. | `x_notification_preferences` is created via runtime `CREATE TABLE IF NOT EXISTS` inside `mountNotify` (`notify.js:131-136`) — **not** in any migration file. This violates the execution plan's own guardrail #5 ("All schema via `migrations/NNN_*` — no engine-created tables"). No test suite covers T1.8.1 at all. |
| **T1.9.1** | Approval engine (policies/chains/delegation/escalation) | **PARTIAL** | `approvals.js` (308 ln): sequential advance (227-244), parallel voting (210-226), delegation via `x_approval_delegations` (125-126,154-169), authority-limit escalation-by-role-append (72-99), maker-checker (183-185); migration `501:30-41`; tested `test-vnext-kernel-completion.mjs:209-261`. | No **timeout**-based escalation exists (zero hits for `escalat\|timeout\|overdue\|sla` beyond the amount-limit path) — roadmap acceptance explicitly requires "timeout escalates." Withdraw-releases-document flow (also a named roadmap acceptance item) not confirmed present — needs explicit verification/implementation. |
| **T1.9.2** | Approval Center UI (nine work queues) | **FAIL — confirmed unbuilt** | Roadmap (`OCTAGON_VNEXT_MASTER_ROADMAP.md:158-161`) specifies the canonical RuoYi-pattern 9 boxes: **my / todo / done / cc / delegated / escalated / withdrawn / rejected / returned**. | `vnext/client/inbox.js` (92 ln) implements exactly **4** boxes (`todo`, `mine`, `done`, `cc` — `inbox.js:55`), backed by `BOXES = new Set(['todo','mine','done','cc'])` server-side (`approvals.js:8`). No other file anywhere in `vnext/client/` implements a multi-queue approval dashboard. **This is the largest single gap in the release and the one both prior audit documents missed entirely by omitting the task row.** |
| **T1.10.1** | Import/export + print templates | **PASS** | `excel.js:141-333` (500-row page size, per-row error report, 4-worker concurrent import); `print.js` RTL confirmed (`dir="rtl"` line 78); `print-templates.js` seeds `tpl_invoice_default` with Arabic labels, mounted `server.js:2854`. | No dedicated server-side bulk-import test file, though client-side logic is sound. |
| **T1.11.1** | Workflow engine (durability/retry/loop-guard) | **PASS** | `workflow-engine.js` (392 ln): restart-recovery (`recoverRunningWorkflows` 299-320, auto-invoked on boot 357); exponential backoff retry (`executeNodeWithRetry` 218-237); depth>10 loop-guard + 60/min rate limit (242-255); mounted `server.js:2855,1824`; tested `test-vnext-kernel-completion.mjs:267-299`. | Retry/loop-guard paths lack dedicated tests (code present, untested). |
| **T1.12.1** | Module/extension framework | **FAIL — dead code** | `ModuleRegistry` class is genuinely implemented: real DFS load-order + circular-dependency detection (`module-framework.js:65-96`), `x_installed_modules` table (`migrations/501:50-54`). | `module-framework` is **required nowhere** in `server.js` (zero matches) — no install/uninstall route, no lifecycle-hook invocation ever fires, zero test coverage. The engine exists but is completely unreachable; the acceptance criterion has no execution path. |
| **T1.13.1** | Org & fiscal masters + company switcher | **FAIL — unwired** | Schema is real and complete (`migrations/401_r1_lane_d_tables.mjs:6-64`: companies/branches/departments/warehouses/locations/currencies/fiscal_periods). | `org-structures.js` exposes only 3 read-only helpers, zero writes, no fiscal-year generator anywhere. Zero matches for `org-structures` in `server.js` — no `/api/org` or `/api/compan*` route exists. No company-switcher UI (`vnext/client/shell/` doesn't exist). The only live company-scoping is a pre-existing, older single-tenant JSON-blob mechanism (`server.js:646-772`, tagged from an earlier wave) — unrelated to this new SQL schema, and provides no HTTP-provable isolation test for it. *(Note: this does not block T1.2.2's row-scope mechanism, which already works independently — company/dept/own scoping for CRUD is functionally present via the older mechanism, just not via the new R1.13 schema.)* |
| **T1.14.1** | Auth hardening (TOTP/policy/keys) | **PARTIAL** | Real and wired: TOTP verify at login (`auth-hardening.js:6-47`, called `server.js:1905-1910`); API-key validation for `x-api-key` header (`49-68`, used `server.js:1510-1516`); user-disable gate (`70-81`, `server.js:1528-1534`); 8h session TTL (`server.js:84,1926,1933`); `x_api_keys` table matches spec (`migrations/501:43-48`). | No password-policy code anywhere. No session rotation (TTL only, zero `rotate` matches). No TOTP **enrollment** route (login checks a pre-existing secret; nothing lets a user set one). No API-key **issuance/revocation** route (keys only ever inserted by the migration/test script, never by a live endpoint). **Test-integrity defect**: `test-vnext-kernel-completion.mjs:306-310` calls `verifyTotp()`, never asserts on the result, and unconditionally prints PASS — this "TOTP E2E" test proves nothing. |

**Tally: 6 PASS · 8 PARTIAL · 5 FAIL/MISSING (of 19).**

---

## 2. Guardrail & Process Findings (separate from task verdicts)

1. **Migration-lane discipline partially broken.** The repair pass added `migrations/501_r1_kernel_completion.mjs` — a single cross-cutting migration touching Lane A (doc-state tables), Lane B (ACL field rules, approval policies/delegations), Lane C (hash columns), and Lane D (module registry, API keys) in one file outside any lane's reserved block (A:100s/B:200s/C:300s/D:400s). This is an execution-plan process violation (§6: "each lane numbers within a reserved block"). It is not being unwound retroactively (that would risk data loss with no git safety net), but any further schema work must return to proper per-lane numbering: next available are **102 (A), 202 (B), 302 (C), 402 (D)**.
2. **Engine-created table outside migrations.** `vnext/server/notify/notify.js:131-136` creates `x_notification_preferences` via runtime `CREATE TABLE IF NOT EXISTS`, violating guardrail #5 ("All schema via migrations — no engine-created tables"). Must be moved into a proper Lane C migration (302).
3. **`VNEXT_PROGRESS.md` is stale.** Last entry is `"R1 AUDIT PARTIAL 2026-07-17"` — the entire 14:07–14:14 repair pass left no ledger record, and per the execution plan "stale entries are defects." This report itself should be logged there once acted on.
4. **Fake test found.** `test-vnext-kernel-completion.mjs:306-310`'s TOTP test never asserts — a false-green signal that would have let T1.14.1 pass a superficial gate check.

## 3. Kernel-Demo Exit Gate — Not Yet Runnable

The roadmap's R1 exit gate requires one migration-seeded `crm_lead` entity to exercise, live over HTTP and browser: registry-driven CRUD, numbering, document states, backend ACL, row scopes, field masking, chatter+attachment security, audit/history, saved views/worklists, custom+snapshot fields, approval, workflow rule, notification, import/export, template print, auth/session enforcement, and module-extension contribution — with zero `crm_lead`-specific server logic.

Given §1, the gate **cannot pass today**: CRUD update is broken (T1.1.2), attachments don't exist (T1.6.1), snapshot fields don't exist (T1.5.1), the module-extension contribution has no execution path (T1.12.1), and the UI needed to demonstrate this "through browser behavior, not filenames" isn't mounted anywhere reachable (T1.1.3). The gate will be run for real, over HTTP and the Browser pane, once the blocking items below are closed — not inferred from code review.

## 4. Priority-Ordered Remaining Work

1. **P0 — regression, blocks everything:** T1.1.2 `cfg` undefined bug (one-line-class fix, but total blast radius).
2. **P0 — blocks gate:** T1.1.3 mount/serve the entity UI at a real navigable page.
3. **P1 — named roadmap invariants unmet:** T1.3.1 posted-immutability + full-graph validation; T1.9.1 timeout-escalation + withdraw; T1.6.1 attachments; T1.9.2 expand inbox.js from 4 to the canonical 9 boxes.
4. **P1 — total misses:** T1.5.1 snapshot fields; T1.12.1 wire module-framework into server.js with real lifecycle routes; T1.13.1 wire org-structures.js with fiscal-year generation + company switcher + isolation test.
5. **P2 — hygiene/completeness:** T1.4.2 client history panel; T1.7.1 scoped worklist counts; T1.8.1 move preferences table into a proper migration; T1.14.1 password policy, session rotation, TOTP enrollment route, API-key issuance route, and fix the fake TOTP test.

R1 does not close, R2 does not start, until §1 shows 19/19 PASS and §3's gate has actually been run and recorded with evidence.
