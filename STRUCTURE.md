# Octagon ERP Structure

This system is built as a single-page application (SPA) with a modular architecture for AI-driven operation.

## Key Files

- `index.html`: Entry point of the ERP system, loading all required modules, stylesheets, and pages.
- `app.js`: Main ERP application logic, handling live store operations and state.
- `style.css`: Core stylesheet for general layout and spacing.
- `omni-ai-assistant.js`: The AI assistant panel wrapper, carrying resizable/movable UI states and layout persistence.
- `jarvis-voice-runtime.js`: Dedicated Voice Runtime for hands-free audio capturing, state machine, and echo loops.
- `jarvis-visual-overlay.css`: Scoped CSS styles for JARVIS pulsing visual highlights and status cards.

## Modules Directory (`modules/`)
- `jarvis-system-map.js`: Scans the DOM and caches ERP pages, forms, and actions.
- `jarvis-action-agent.js`: Highlights active targets and executes deterministic action IDs safely.
- `jarvis-test-harness.js`: Diagnostics self-test modal, programmatic matrix, and report exporter.
- `ai-providers.js`: Standardized provider routing (OpenRouter / Gemini fallbacks).
- `jarvis-brain.js`: Planner/executor coordinating user requests with direct tools and approval center requests.
- `ai-governance.js`: Registers tool permissions and policies, auditing all AI operations.
- `pos.js`, `mrp.js`, `workshop-ai.js`, `route-health.js`, etc.: Sector-specific business and diagnostic logic.

## Services Directory (`services/`)
- `financeService.js`, `stockService.js`, `recordService.js`, `tenantService.js`: State managers handling the ERP database.

> **Phase 8A (2026-06-26) — Release Candidate audit:** route baseline confirmed clean at **93/93/93** (nav / view markers / counted routed view files), total view files **95**, internal route-less **2** (`manager_approvals`, `mobile_inventory_count`). PAGE_METADATA and PAGE_PERMISSIONS both now cover **93/93** nav pages (Phase 8A added 10 workshop/core pages to PAGE_METADATA in `services/permissionService.js`). See `docs/RELEASE_CANDIDATE_PILOT_CHECKLIST.md`.

## Page / Route Registry (reconciled 2026-07-03)
A page is "fully wired" when it appears consistently in **six** places. As of this pass all **95** sidebar pages agree (79 after the 2026-06-22 reconciliation; +`appointments` +`loyalty` 06-22; +`esign` +`events` +`knowledge` +`surveys` +`visitors` +`risk_compliance` 06-23; +`telegram` 06-26 approved by Saif; +Phase 7J/7L `sales_commission` `sales_contracts` `sales_price_lists` `finance_installments` `pos_deepening` `omni_communications` `integration_hub` 06-26; +Phase 8D `knowledge_base` 07-03):

| Registry | Where | Count | Role |
| --- | --- | --- | --- |
| Sidebar buttons | `index.html` `.nav-btn[data-page]` | 95 | what the user can click |
| View markers `<!-- view:key -->` | `index.html` | 95 | **documentary manifest only** — NOT parsed at runtime (only the build-time `scratch/extract_views.js` reads them) |
| Page templates | `views/<key>.html` | 95 | the actual `<section class="page">`, fetched on demand. **Filename must equal the page key** (underscores, e.g. `sales_commission.html`) or the loader 404s → blank page |
| Dynamic loader map | `app.js` → `ensurePageTemplateLoaded()` `pageMap` + `prefetchAllViews()` | 95 | appends each template into `#mainContent` and warms them on boot |
| Renderer | core `app.js` (`switchPage` inline `pageMap`, 31 core pages) **or** a module that wraps `window.switchPage` (enterprise-suite, route-health, appointments…) | 95 | hydrates the page body |
| Route Health | `modules/route-health.js` (runtime) | 95/95 | audits nav↔section↔renderer live |

> Total `views/*.html` files = **97** (95 routed/counted + 2 internal route-less: `manager_approvals`, `mobile_inventory_count`). Previous baseline was 89 total before the Knowledge Base page and route-reconciliations.

Notes:
- **Route Health is a dynamic metric**: it enumerates live `.nav-btn[data-page]` and checks each has a matching section + render hook. Its denominator therefore **equals the current sidebar size**. Older audits that said "66/66" were point-in-time snapshots taken before later pages (warranty, the 11 enterprise-suite tabs, appointments, …) were added — not a cap or a regression.
- **Permissions** (`services/permissionService.js` `PAGE_PERMISSIONS`) now map the first Phase 6E deployment-hardening batch explicitly: finance, HR/payroll, AI/system, admin/security, tenant/data quality, procurement/approvals, and diagnostic pages. Current Security Center audit shows **53 mapped sidebar pages**; normal business/vertical/self-service pages that are not yet policy-critical remain local/dev default-allow by design.
- The `switchPage` inline `pageMap` (31 entries) covers only the original core pages; the remaining ~55 are routed by the dynamic loader + per-module `switchPage` wrappers. Both together cover all 86.
- **Governance freeze 2026-06-23 / Phase 6E update 2026-06-24:** `risk_compliance` is an active routed page and participates in the 86/86 baseline. It is not fully read-only: it can persist self-contained governance records under `omni.riskCompliance` when the user adds/updates risks or controls. It does not post financial/accounting/payroll/stock/QC/price changes and does not mutate locked payroll/finance pages. It is now explicitly page-mapped to manager roles and still guards writes through `risk_compliance.write`.
- **Handover protocol:** `MASTER_ROADMAP.md` is the long-range single source of truth; `HERE.md` is the short live handoff; `STRUCTURE.md` is the route/page registry. `CODEX_RUNBOOK.md` and `all into here file.md` are not present in the current root and were not restored during the 2026-06-23 governance freeze because the current `MASTER_ROADMAP.md` states they were consolidated/replaced. Do not recreate parallel handoff files unless the project protocol is explicitly changed.
- **Deployment permission risk:** Phase 6E reduced the highest-risk default-allow surface, but this is still not production authentication. Before live deployment, add real sessions/password or external identity, finish explicit policy for the remaining business pages, and run seeded-role regression tests.
- **Loader race fix (2026-06-23):** `ensurePageTemplateLoaded()` had a race — the default boot page (calculator) was loaded by initial navigation AND by `prefetchAllViews()` concurrently; both passed the existence check before either appended, producing a duplicate `pageCalculator` section (Route Health read 82). Fixed with an in-flight `Set` guard (`window.__viewLoadsInFlight`) plus a re-check of `getElementById(section.id)` immediately before `appendChild`. Sections are now exactly 1 per page.
- **Boot "startup interrupted" fix (2026-06-23):** the boot UI-refresh ran the calculator recompute trio (`validateDays`/`autoCalcEligibleFridays`/`recalculate`) before the lazily-loaded page section was mounted → `null.value` ("Octagon startup interrupted" console error on every boot). Fixed defensively: null-guards in `validateDays`/`autoCalcEligibleFridays`, and the call site (~line 11041) now only runs the trio when `getElementById('inpAttendance')` exists. No payroll-logic change. Console is now clean on boot.
- **Cache-busting:** static assets are versioned by `?v=` query in index.html. When editing a long-cached file (e.g. `app.js`), bump its `?v=` or the dev server/browser may serve the stale copy (symptom: console stack-trace line numbers don't match your edited file).
- **Phase 6A core ERP gap slice (2026-06-23):** no new pages were added; Route Health remains 86/86. `modules/phase6a-core.js` and `modules/phase6a-core.css` inject three governed surfaces into existing pages: Bank Reconciliation inside `banking`, Storage Locations inside `inventory`, and Chart of Accounts inside `admin_panel`.
- **Phase 6A data policy:** bank matching writes metadata only under `omni.banking.*` and routes differences to Command Center requests; storage locations use `omni.warehouses[]`, `omni.storageLocations[]`, `omni.locationStock[]`, and `omni.locationMovements[]` without changing old material totals; Chart of Accounts reads `finance.accounts` and can add safe unused accounts without changing posted entries.
- **Phase 6A permissions:** `risk_compliance` and `banking` now have explicit `PAGE_PERMISSIONS`. Sensitive model mappings were added for `finance.accounts`, `omni.banking`, and `omni.locationStock`. This is intentionally minimal; full permission hardening for all 86 pages remains a future deployment gate.
- **Phase 6B action hardening (2026-06-23):** `services/permissionService.js` now exposes `ACTION_PERMISSIONS`, `checkAction()`, and `requireAction()` for sensitive Phase 6A/governance actions without changing page default-allow behavior. `modules/phase6a-core.js` guards bank reconciliation, storage-location movements, COA account creation, old direct banking finalize/create paths, and writes allowed/blocked/approval_requested audit events. `modules/risk-compliance.js` guards risk/control writes through `risk_compliance.write`.
- **Phase 6B deployment risk report:** explicit page mappings remain limited to sensitive/core pages; unmapped pages still default-allow for local/dev. Before live deployment the project must decide the login/current-user model, map all sensitive pages/actions, convert high-risk writes to deny-by-default, and test system/admin, manager, operator, finance, and ordinary user roles.
- **Phase 6C Admin audit + prefetch recovery (2026-06-23):** Admin Wire-Up now includes a live Button / Permission Audit panel that checks all 86 sidebar buttons for target sections, click wiring, explicit/default permission status, and guarded action count. The end-of-body dynamic view prefetch wrapper now starts immediately when `DOMContentLoaded` already fired, recovering the full 86/86 Route Health baseline on normal load. Latest browser smoke: Route Health 86/86 nav and 86/86 pages; Admin audit 86 OK, 0 warnings, 0 broken, 15 guarded actions.
- **Phase 6C role/action matrix + high-risk action policy (2026-06-23):** `PermissionService.explainAction(actionKey, context, userOrRole)` provides dry-run outcomes for sensitive actions. Page-level default-allow remains for local/dev, but high/critical unmapped actions are no longer silently allowed; they explain as blocked or approval-required. `modules/phase6c-security-matrix.js` injects a read-only matrix into the existing `security_center` page, covering banking reconciliation, inventory locations, Chart of Accounts, Risk Compliance, direct finance posting, and AI high-risk write keys. Approval requests from Phase 6A now carry action key, source page/module, user/role, target id, before/after, risk level, requested timestamp, status, and reason. Audit/history entries now include role, risk level, and reason alongside action/result/target.
- **Phase 6D real-user foundation and stable baseline (2026-06-23):** the project now has a local-only Git baseline (`ae8e614 stable-86-route-health-phase6c`) created after syntax/JSON validation and a backup marker under `db-backups/`. Runtime normalization seeds safe development identities under `omni.users`, `omni.roles`, and `omni.userRoles` when missing: `system_admin`, `finance_manager`, `workshop_manager`, `operator_user`, `employee_user`, and `viewer_user`. No passwords are seeded. `authUserSwitcher` is data-backed by `omni.users` with fallback profiles preserved. `PermissionService.resolveGroups()` now resolves `groups`, `roleId`, or `role` through `omni.roles[].groups`. The `security_center` matrix tests current user + real users + fallback profiles and includes HR/payroll high-risk action dry-runs. A documentation-only 86-page permission sensitivity audit is rendered in the same page; page default-allow remains unchanged until Phase 6E.
- **AI current-user enforcement (Phase 6D):** Jarvis and AI approval queues now stamp `requestedBy`, `requestedById`, `requestedByRole`, payload user fields, and source (`jarvis_brain`, `ai_control_dashboard`, `ai_system_chat`, `omni_ai_assistant`, or HR/automation source) on approval-routed items. High-risk direct AI tools remain approval-gated; this phase did not enable silent finance, stock, COA, payroll, or system writes.
- **Phase 6E controlled page hardening (2026-06-24):** `PermissionService.explainPage(page, userOrRole)` and `PAGE_METADATA` now explain page-level access decisions. The first explicit page-policy batch covers admin/security, HR/payroll, finance, AI/system, tenant/data quality, procurement/approvals, diagnostics, and selected self-service pages. `modules/phase6c-security-matrix.js` now renders the Phase 6E page-policy audit inside `security_center`, showing mapped/default status, sensitivity, current-user access, and phase. `modules/route-health.js` now hydrates all view templates only when Route Health runs, preserving fast boot while restoring full **86/86 nav + 86/86 pages** proof on demand.
- **Phase 6F seeded-role regression harness (2026-06-24):** `scripts/permission-regression.mjs` is now the read-only automated regression for the Phase 6D/6E permission baseline. It verifies six seeded roles, role-group inheritance through `omni.roles`, 86 sidebar pages, 53 mapped sidebar pages, mapped page allow/block outcomes, high-risk action approval routing, and unmapped local/dev default-allow behavior. Latest run: 35/35 PASS. Live browser audit also passed: Route Health 86/86, Workshop Stabilization 12/12, Security Center page-policy audit visible, and console clean after the inline favicon removed `/favicon.ico` 404 noise.
- **Phase 6G legal / AI order / maintenance coverage (2026-06-25):** no new sidebar pages were added. `modules/documents.js` now covers printable A4 legal drafts for employee contracts, company internal rules, and per-document metadata sheets from the existing Documents page. `app.js` adds a structured AI-assisted order/task form inside Intelligence: AI can draft wording, then issue a safe Task Manager task or Command Center approval/order request with current-user context. Maintenance coverage remains split across Machines (hour/day intervals + Task Manager maintenance request), Assets (preventive-maintenance due schedule and logs), and Equipment (monthly audit/status and dispatch/return condition logs). Automatic recurring generation for due asset/equipment maintenance remains a next hardening step.
- **Phase 6H authentication hardening and page mapping (2026-06-25):** Mapped all remaining 33 default-allow pages in `services/permissionService.js`, achieving 100% explicit page permissions coverage (86/86 pages). Client-side authentication uses browser-native Web Crypto SHA-256 + salt to secure seeded user credentials. Guest session state blocks unauthorized access except on public/kiosk pages, and the top-bar user switcher `#authUserSwitcher` is restricted to devMode/admins (with a password reset flow added to the admin panel). Automated regression harness updated to expect 86/86 mapped pages and runs cleanly at 35/35.
- **Phase 7A stabilization foundation (2026-06-26):** No sidebar pages were added; `index.html` now loads `modules/phase7a-stabilization.js` after the Phase 6 security matrix. `server.js` exposes a minimal server auth/session bridge (`/api/auth/login`, `/api/auth/session`, `/api/auth/logout`), read-only release status (`/api/release/status`), and backup dry-run verification (`/api/backup/verify`). `app.js` bridges successful local login/logout into the server session when credentials are available. The Phase 7A module injects panels into existing `security_center`, `deploy_ready`, `data_quality`, and `finance` surfaces; it also wraps new `addFinanceTransaction()` writes with a manual period-lock check. New runtime keys are non-destructive: `omni.phase7a`, `omni.phase7aAudit`, `finance.periodLocks` / `omni.finance.periodLocks`.
- **Phase 7A backup note:** The existing backup API creates `database.backup.*.json` in the project root. Phase 7A corrected backup verification under SQLite mode so backups are verified against the active DB source rather than stale `database.json`. Local backup snapshots are intentionally ignored by Git.
- **Phase 7B production safety closure (2026-06-26):** No sidebar pages were added; route/page registry remains 86/86. `server.js` now reports sanitized port/process diagnostics through `/api/server/status` and includes server/API protection state in `/api/release/status`. Sensitive API writes are guarded by local/dev-aware session, admin, or role gates; WhatsApp webhook verification/signature behavior is preserved. Backup restore now has `/api/restore/dry-run` for non-mutating key/count comparison, while destructive `/api/restore` requires admin context and typed confirmation. `modules/phase7a-stabilization.js` remains the injection module but now renders Phase 7B readiness, port, backup dry-run, remote Git, and production data-quality blocker details inside existing surfaces.
- **Phase 7D report designer and smart views (2026-06-26):** No sidebar pages were added; route/page registry remains 86/86. `modules/nl-reporting.js` now extends the existing `nl_reports` page with saved report definitions under `omni.nlReports.definitions`, selected-column smart views, role-group access metadata, client-side CSV/JSON/Excel export, Arabic print rendering, and disabled scheduler metadata under `omni.nlReports.schedules`. AI drafting is intentionally read-only: it only proposes a definition title from the current report intent and never mutates source finance, payroll, stock, or reporting rows. Cache keys for `modules/nl-reporting.js` and `.css` were bumped in `index.html`.
- **Telegram connector page (2026-06-26, live feature request approved by Saif):** This is an **intentional** route-count change — the first sidebar page added since the governance freeze. Baseline moves **86 → 87** (nav buttons, view templates, dynamic loader, renderers, Route Health) and total `views/*.html` **88 → 89**. New page key `telegram` (Arabic `تلغرام`, English "Telegram Connector"), placed in the `النظام` group near `whatsapp`/`automation`. It is a **safe foundation only**: bot status / setup metadata / message inbox / approval-gated outbound queue / automation use-case tiles / Jarvis panel. No real bot token is shown or stored client-side or in `database.json`; the real token must stay server-side (`TELEGRAM_BOT_TOKEN`). No outbound message is ever auto-sent — everything is draft/approval-routed. Jarvis is page-aware (page-truth registry, triggers, starter prompts, context, attention surfacing) but can only **draft**; it cannot send. Registered in: `views/telegram.html`, `index.html` nav, `app.js` (`renderTelegramIntegrationPage` + `normalizeTelegramData` + page maps/prefetch), `modules/data-providers.js` page-truth registry, `services/permissionService.js` (`PAGE_METADATA` + `PAGE_PERMISSIONS` → `workshop.manager`/`finance.manager`), `omni-ai-assistant.js`, and `scripts/permission-regression.mjs` (expected counts 86 → 87). Backend `/api/telegram/*` endpoints are intentionally **deferred** to a future server-side slice; `server.js` was not modified so existing WhatsApp webhook behavior is untouched. Data foundation under `omni.telegram` (`config`, `chats`, `inbox`, `outboundQueue`, `messageLinks`, `automationRules`, `activityLog`, `status`) is seeded non-destructively (defaults only if missing).
